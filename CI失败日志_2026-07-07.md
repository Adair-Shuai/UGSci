# CI 失败日志汇总 — 2026-07-07

## 1. Desktop Manual Build (#18)

**Commit:** `bb07d5f7f7`
**Run URL:** https://github.com/Adair-Shuai/UGSci/actions/runs/28858619339

### Build Desktop App (Windows / macOS) — 5 errors (两个平台一致)

```
Build failed with 5 errors:

[MISSING_EXPORT] "default" is not exported by "../../src/features/SkillStore/SkillList/ImportFromGithubModal.tsx".
    → ../../src/features/UgsShared/AddSkillButton.tsx:8:8
      import ImportFromGithubModal from "@/features/SkillStore/SkillList/ImportFromGithubModal";

[MISSING_EXPORT] "default" is not exported by "../../src/features/SkillStore/SkillList/ImportFromUrlModal.tsx".
    → ../../src/features/UgsShared/AddSkillButton.tsx:9:8
      import ImportFromUrlModal from "@/features/SkillStore/SkillList/ImportFromUrlModal";

[MISSING_EXPORT] "default" is not exported by "../../src/features/SkillStore/SkillList/UploadSkillModal.tsx".
    → ../../src/features/UgsShared/AddSkillButton.tsx:10:8
      import UploadSkillModal from "@/features/SkillStore/SkillList/UploadSkillModal";

[MISSING_EXPORT] "emailOtp" is not exported by "../../src/libs/better-auth/auth-client.desktop.ts".
    → ../../src/features/Auth/SignIn/useSignIn.ts:9:10
      import { emailOtp, phoneNumber, requestPasswordReset, signIn } from "@/libs/better-auth/auth-client";

[MISSING_EXPORT] "phoneNumber" is not exported by "../../src/libs/better-auth/auth-client.desktop.ts".
    → ../../src/features/Auth/SignIn/useSignIn.ts:9:20
      import { emailOtp, phoneNumber, requestPasswordReset, signIn } from "@/libs/better-auth/auth-client";
```

---

## 2. Test CI (#38)

**Commit:** `bb07d5f7f7`
**Run URL:** https://github.com/Adair-Shuai/UGSci/actions/runs/28858448874

### Job: Test Database — Lint step 失败

Lint 命令 `bun run lint` 中 `lint:ts` (eslint) 返回 exit code 1。错误类型涉及大量文件：

- `simple-import-sort/imports` — 需要 autofix 排序的 import（~30+ 文件）
- `no-restricted-imports` — 'Button' / 'Select' import 来自 `@lobehub/ui` 而非 `@lobehub/ui/base-ui`
- `unused-imports/no-unused-imports` — 未使用的 import
- `unused-imports/no-unused-vars` — 未使用的变量
- `perfectionist/sort-interfaces` — interface 字段排序
- `perfectionist/sort-enums` — enum 排序
- `react-hooks/rules-of-hooks` — `DesktopAutoOidcOnFirstOpen.tsx:22` 条件性调用 Hook
- `import-x/no-duplicates` + `import-x/first` — `toolAvailability.test.ts`
- `unicorn/new-for-builtins` — 用 `Array()` 而非 `new Array()`
- `simple-import-sort/exports` — 导出排序

完整文件列表（ESLint error，非 warning）见 CI log。

---

### Job: Test Packages — 5 test failures

**Package:** `@lobechat/electron-server-ipc`

**src/ipcClient.test.ts** — 2 failed:

1. `ElectronIpcClient > initialization > should initialize with socket path from info file if it exists`
   - 期望: `"/mock/temp/dir/lobehub-electron-ipc-info.json"`
   - 实际: `"/mock/temp/dir/ugsci-electron-ipc-info.json"`

2. `ElectronIpcClient > initialization > should initialize with default socket path if info file does not exist`
   - 期望: `"/mock/temp/dir/lobehub-electron-ipc-info.json"`
   - 实际: `"/mock/temp/dir/ugsci-electron-ipc-info.json"`

**src/ipcServer.test.ts** — 3 failed:

3. `ElectronIPCServer > server initialization and start > should create server and start listening`
   - 期望: `"/mock/temp/dir/lobehub-electron-ipc.sock"`
   - 实际: `"/mock/temp/dir/ugsci-electron-ipc.sock"`

4. `ElectronIPCServer > server initialization and start > should remove existing socket file if it exists`
   - 期望: `"/mock/temp/dir/lobehub-electron-ipc.sock"`
   - 实际: `"/mock/temp/dir/ugsci-electron-ipc.sock"`

5. `ElectronIPCServer > server close > should close server and clean up socket file`
   - 期望: `"/mock/temp/dir/lobehub-electron-ipc.sock"`
   - 实际: `"/mock/temp/dir/ugsci-electron-ipc.sock"`

**原因:** fork 重命名后 (`lobehub` → `ugsci`)，测试 mock 中 IPC 路径常量未更新。

---

### Job: Test Server (shard 2/2) — 2 test failures

**File:** `apps/server/src/services/bot/platforms/__tests__/const.test.ts`

1. `normalizeBotReplyLocale > falls back to en-US when the input is not a project locale`
   - 期望: `"en-US"`
   - 实际: `"zh-CN"`

2. `getBotReplyLocale > falls back to en-US for unknown or missing platforms`
   - 期望: `"en-US"`
   - 实际: `"zh-CN"`

**原因:** fork 中默认语言被改为 zh-CN，locale 回退逻辑期望与默认值不一致。

---

### Job: Test App (shard 2/2) — 10 test failures (4 files)

**1. `src/features/Auth/SignIn/useSignIn.test.ts` — 2 failed**

- `handleForgotPassword > should show error on failure`
  - 期望 step: `"password"` → 实际: `"email"`
- `magic link > should land on email-sent state when a passwordless user triggers magic link`
  - 期望 `mockSignInMagicLink` 被调用 1 次 → 实际 0 次

**原因:** auth 流程 mock 期望与 fork 自定义行为不匹配。

**2. `src/features/ModelSwitchPanel/components/ModelDetailPanel.test.tsx` — 4 failed**

- `renders branding provider token pricing in credits`
  - 期望: `"5M credits/M tokens"` → 实际: `"$5.00/M tokens"`
- `renders the original branding price without repeating the unit suffix`
  - 期望: `"5M"` → 实际: `"$5.00"`
- `renders branding provider image and video pricing in credits`
  - 期望: `"~ 40.0K credits / image"` → 实际: `"~ $0.04 / image"`
- `renders a placeholder for empty lookup pricing tables`
  - 期望: `"- credits/img"` → 实际: `"$-/img"`

**原因:** fork 自定义了 business pricing 逻辑（显示美元而非 credit），测试期望未同步。

**3. `src/features/ModelSwitchPanel/hooks/useModelDetailPanel.test.ts` — 2 failed**

- `applies business pricing before formatting LobeHub credit prices`
  - 期望 true → 实际 false
- `formats original unit prices for tiered and lookup units`
  - 期望: `{current: "20.0K", original: "50.0K"}` → 实际: `{current: "0.02", original: "0.05"}`

**原因:** 同上，business pricing 逻辑导致 credit 换算失效。

**4. `src/app/spa-auth/[locale]/[[...path]]/seoMeta.test.ts` — 2 failed**

- `buildAuthSeoEntry > uses hand-translated zh-CN keys`
  - 期望 title: `"登录"` → 实际: `"Sign In"`
- `buildSeoMeta > normalizes hostile locale input to an allowlisted value`
  - 期望包含 `property="og:locale" content="en-US"` → 实际输出为英文

**原因:** fork 中 zh-CN 翻译 key 未配置或未生效；locale normalization 逻辑与原版 LobeHub 不一致。

---

---

# 第二次构建 — 2026-07-07 (commit `5db9025e`)

## 1. Desktop Manual Build (#19)

**Run URL:** https://github.com/Adair-Shuai/UGSci/actions/runs/28863714237

### 与上次对比：改善项
- 上次的 5 个 `MISSING_EXPORT` 错误已修复 ✅

### 本次错误（macOS + Windows 一致，共 1 个）

```
Build failed with 1 error:

Error: [vite]: Rolldown failed to resolve import "react-router-dom"
    from "src/routes/(main)/ugs-capabilities/_layout/index.tsx".
```

**原因:** `ugs-capabilities/_layout/index.tsx` 中 `import ... from 'react-router-dom'` 在 electron 构建环境下无法解析。`react-router-dom` 是 SPA 的依赖，electron renderer 可能通过别名引入。

---

## 2. Test CI (#39)

**Run URL:** https://github.com/Adair-Shuai/UGSci/actions/runs/28862864795

### 与上次对比

| Job | 上次 (#38) | 本次 (#39) | 变化 |
|-----|-----------|-----------|------|
| Test Desktop App | ✅ | ✅ | 正常 |
| Test Packages | ❌ 5 failures | ✅ 0 failures | **已修复** |
| Test Database | ❌ lint | ❌ lint | 未变 |
| Test Server (shard 1/2) | ✅ (未运行) | ❌ 4 failures | **新增** |
| Test Server (shard 2/2) | ❌ 2 failures | 🚫 cancelled (因 shard 1 失败) | - |
| Test App (shard 2/2) | ❌ 10 failures | ❌ 1 failure | **大幅减少** |
| Test App (shard 1/2) | 🚫 cancelled | 🚫 cancelled | - |

### Test Server (shard 1/2) — 4 failures（3 个 test 文件）

**1. `apps/server/src/modules/AssistantStore/index.test.ts` — 2 failures**

- `AssistantStore > should return the default index URL when no language is provided`
  - 期望: `.../index.en-US.json`
  - 实际: `.../index.zh-CN.json`

- `AssistantStore > should return the agent URL with default language when no language is provided`
  - 期望: `.../agent-123.en-US.json`
  - 实际: `.../agent-123.zh-CN.json`

**原因:** fork 默认语言改为 zh-CN，AssistantStore 默认语言回退逻辑从 `en-US` 变为 `zh-CN`。

**2. `apps/server/src/modules/PluginStore/index.test.ts` — 1 failure**

- `PluginStore > should return the default index URL when no language is provided`
  - 期望: `.../index.en-US.json`
  - 实际: `.../index.zh-CN.json`

**原因:** 同上，默认语言改为 zh-CN。

**3. `apps/server/src/modules/AgentRuntime/__tests__/RuntimeExecutors.test.ts` — 1 failure**

- `RuntimeExecutors > stream error detection in call_llm > should disable llm execution retry for the branding provider`
  - 期望 throw `'network timeout'`
  - 实际 throw `'Model returned an empty completion (no content, no tool calls, no output tokens).'`

**原因:** Mock LLM 调用返回了空内容而非模拟的网络超时，可能 mock 数据或 provider 行为变化。

### Test App (shard 2/2) — 1 failure（之前 10 个）

**`src/features/ModelSwitchPanel/hooks/useModelDetailPanel.test.ts`**

- `useModelDetailPanel > uses the enabled model list hook when no list is provided`
  - 期望: `'test-model'`
  - 实际: `undefined`

**原因:** enabled model list hook 返回的 model 列表为空，model.id 为 undefined，mock 数据配置与 fork 自定义逻辑不匹配。
